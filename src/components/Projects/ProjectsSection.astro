---
import Projects from "../../config/Projects";
import ProjectCard from "./ProjectCard.astro";
---
<section>
    <div class="w-full px-5 mt-20 flex flex-col items-center justify-center mb-4 gap-5 flex-wrap md:flex-row">
        {
            Projects.map((project) => (
                <ProjectCard
                    title={project.title}
                    desc={project.Desc}
                    link={project.link}
                    repo={project.repo}
                    frame={project.frame}
                    lenguage={project.lenguage}
                    img={project.img}
                />
            ))
        }
    </div>
</section>
